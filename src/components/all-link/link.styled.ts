import styled from "@emotion/styled";
import { css } from "@emotion/react";
import { Link } from 'react-router-dom';

export const Connection = styled(Link) `
    color: inherit;
    text-decoration: inherit;
`